# Pacote base da Lambda — apenas o necessário para FastAPI + rotas funcionarem
# scikit-learn, pandas, numpy, ultralytics, opencv → adicionar como Lambda Layer depois
fastapi==0.115.5
mangum==0.19.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.12
boto3==1.35.74
httpx==0.28.0
requests==2.32.3
