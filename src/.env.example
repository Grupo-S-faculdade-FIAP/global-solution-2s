# LEGADO — use o .env.example na RAIZ do repositório.
# O config carrega primeiro: <repo>/.env, depois src/.env
#
# Copie na raiz:
#   cp .env.example .env
#
# Este arquivo permanece apenas para compatibilidade com setups antigos.
