[pytest]
asyncio_default_fixture_loop_scope = function
pythonpath = src
markers =
    e2e: testes de browser Playwright (requer chromium instalado; excluídos de make test)

[tool:pytest]
# alias for editors that read pyproject only
